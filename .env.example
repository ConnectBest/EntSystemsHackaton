# Environment Variables for Tier-0 Enterprise SRE System

# Cohere API Key (Optional - system works without it using simulated embeddings)
# Get your free API key at: https://cohere.com/
COHERE_API_KEY=your_cohere_api_key_here

# Note: All other environment variables are configured with sensible defaults
# in docker-compose.yml and do not need to be modified for basic usage.
